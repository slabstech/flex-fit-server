# docker-compose.yml
services:
  attendance-app:
    build: .
    container_name: qr-attendance-system
    restart: unless-stopped
    ports:
      - "80:8000"
    # ← NO VOLUMES AT ALL → fresh DB on every restart

networks:
  default:
    driver: bridge